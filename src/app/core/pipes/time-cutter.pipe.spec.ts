import { TimeCutterPipe } from './time-cutter.pipe';

describe('TimeCutterPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeCutterPipe();
    expect(pipe).toBeTruthy();
  });
});
